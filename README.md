# To-do
Lista de Tarefas
Aplicação web simples em Flask para gerenciar uma lista de tarefas (To-Do). 
Permite listar, adicionar, marcar como concluída, editar e deletar tarefas. 
Estilos em `static/styles.css` e templates em `templates/`.


